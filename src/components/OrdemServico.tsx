import { useState, useEffect } from 'react' import { supabase } from '../lib/supabase'  interface Props {   atendimentoId: string<br/>   onBack: () => void }  interface AtendimentoOS {   id: string<br/>   numero: number<br/>   status: string<br/>   tipo_atendimento: string<br/>   subtipo_revisao: string | null<br/>   responsavel_nome: string<br/>   responsavel_telefone: string<br/>   relato_entrada: string<br/>   garantia_status: string<br/>   hora_chave: number | null<br/>   hora_motor: number | null<br/>   hora_eletrica: number | null<br/>   created_at: string<br/>   clientes: { razao_social: string; cnpj_cpf: string | null } | null<br/>   veiculos: { placa_traseira: string } | null<br/>   equipamentos: { modelo: string; numero_serie: string } | null }  interface PecaOS {   id: string<br/>   codigo_peca: string | null<br/>   descricao: string<br/>   quantidade: number<br/>   unidade: string<br/>   observacao: string | null }  interface ServicoOS {   id: string<br/>   codigo_servico: string | null<br/>   descricao: string<br/>   horas_padrao: number | null<br/>   quantidade: number<br/>   unidade: string<br/>   observacao: string | null }  interface Validacao {   acao: string<br/>   observacao: string | null<br/>   created_at: string }  export default function OrdemServico({ atendimentoId, onBack }: Props) {   const [atendimento, setAtendimento] = useState<AtendimentoOS | null>(null)   const [pecas, setPecas] = useState<PecaOS[]>([])   const [servicos, setServicos] = useState<ServicoOS[]>([])   const [validacoes, setValidacoes] = useState<Validacao[]>([])   const [loading, setLoading] = useState(true)    useEffect(() => { carregar() }, [])    async function carregar() {     const { data: atend } = await supabase       .from('atendimentos')       .select(`         id, numero, status, tipo_atendimento, subtipo_revisao, responsavel_nome,         responsavel_telefone, relato_entrada, garantia_status, hora_chave, hora_motor,         hora_eletrica, created_at,         clientes ( razao_social, cnpj_cpf ),         veiculos ( placa_traseira ),         equipamentos ( modelo, numero_serie )       `)       .eq('id', atendimentoId)       .single()      if (atend) setAtendimento(atend as unknown as AtendimentoOS)      // CORRIGIDO: itens_execucao com filtro tipo='PECA'<br/>     const { data: pecasData } = await supabase       .from('itens_execucao')       .select('id, codigo_peca, descricao, quantidade, unidade, observacao')       .eq('atendimento_id', atendimentoId)       .eq('tipo', 'PECA')       .order('descricao')      if (pecasData) setPecas(pecasData)      // CORRIGIDO: itens_execucao com filtro tipo='SERVICO'<br/>     const { data: servicosData } = await supabase       .from('itens_execucao')       .select('id, codigo_servico, descricao, horas_padrao, quantidade, unidade, observacao')       .eq('atendimento_id', atendimentoId)       .eq('tipo', 'SERVICO')       .order('descricao')      if (servicosData) setServicos(servicosData)      const { data: valData } = await supabase       .from('validacoes_gestor_oficina')       .select('acao, observacao, created_at')       .eq('atendimento_id', atendimentoId)       .order('created_at')      if (valData) setValidacoes(valData)      setLoading(false)   }    if (loading) {     return <div className="min-h-screen flex items-center justify-center bg-gray-50"><div className="text-gray-500">Carregando...</div></div>   }    const tempoTotal = servicos.reduce((acc, s) => acc + (s.horas_padrao || 0) * s.quantidade, 0)    return (     <div className="min-h-screen bg-gray-50">       <header className="bg-blue-900 text-white px-4 py-4 flex items-center gap-4">         <button onClick={onBack} className="text-blue-200 hover:text-white">← Voltar</button>         <h1 className="text-lg font-bold">Ordem de Serviço #{atendimento?.numero}</h1>       </header>        <main className="max-w-3xl mx-auto px-4 py-6 space-y-6">         {/
Identificação
/}         <div className="bg-white rounded-xl shadow-sm p-5">           <h2 className="font-bold text-gray-800 mb-3">Identificação</h2>           <div className="grid grid-cols-2 gap-3 text-sm">             <div><p className="text-gray-400">Cliente</p><p className="text-gray-800">{atendimento?.clientes?.razao_social || '—'}</p></div>             <div><p className="text-gray-400">CNPJ/CPF</p><p className="text-gray-800">{atendimento?.clientes?.cnpj_cpf || '—'}</p></div>             <div><p className="text-gray-400">Responsável</p><p className="text-gray-800">{atendimento?.responsavel_nome || '—'}</p></div>             <div><p className="text-gray-400">Telefone</p><p className="text-gray-800">{atendimento?.responsavel_telefone || '—'}</p></div>             <div><p className="text-gray-400">Placa</p><p className="text-gray-800">{atendimento?.veiculos?.placa_traseira || '—'}</p></div>             <div><p className="text-gray-400">Modelo</p><p className="text-gray-800">{atendimento?.equipamentos?.modelo || '—'}</p></div>             <div><p className="text-gray-400">Série</p><p className="text-gray-800">{atendimento?.equipamentos?.numero_serie || '—'}</p></div>             <div><p className="text-gray-400">Tipo</p><p className="text-gray-800">{atendimento?.tipo_atendimento === 'REVISAO' ? `Revisão ${atendimento?.subtipo_revisao || ''}` : 'Serviço Comum'}</p></div>             <div><p className="text-gray-400">Hora-chave</p><p className="text-gray-800">{atendimento?.hora_chave || '—'}</p></div>             <div><p className="text-gray-400">Hora-motor</p><p className="text-gray-800">{atendimento?.hora_motor || '—'}</p></div>             <div><p className="text-gray-400">Hora-elétrica</p><p className="text-gray-800">{atendimento?.hora_eletrica || '—'}</p></div>             <div><p className="text-gray-400">Garantia</p><p className="text-gray-800">{atendimento?.garantia_status || '—'}</p></div>           </div>         </div>          {/
Relato do cliente
/}         {atendimento?.relato_entrada && (           <div className="bg-white rounded-xl shadow-sm p-5">             <h2 className="font-bold text-gray-800 mb-2">Relato do Cliente</h2>             <p className="text-gray-700 text-sm">{atendimento.relato_entrada}</p>           </div>         )}          {/
Peças e materiais
/}         {pecas.length > 0 && (           <div className="bg-white rounded-xl shadow-sm p-5">             <h2 className="font-bold text-gray-800 mb-3">Peças e Materiais</h2>             <table className="w-full text-sm">               <thead>                 <tr className="border-b border-gray-200">                   <th className="text-left py-2 text-gray-400 font-medium">Código</th>                   <th className="text-left py-2 text-gray-400 font-medium">Descrição</th>                   <th className="text-right py-2 text-gray-400 font-medium">Qtd</th>                   <th className="text-right py-2 text-gray-400 font-medium">Un</th>                 </tr>               </thead>               <tbody>                 {pecas.map((p) => (                   <tr key={p.id} className="border-b border-gray-50">                     <td className="py-2 text-gray-800">{p.codigo_peca || '—'}</td>                     <td className="py-2 text-gray-800">{p.descricao}</td>                     <td className="py-2 text-right text-gray-800">{p.quantidade}</td>                     <td className="py-2 text-right text-gray-800">{p.unidade}</td>                   </tr>                 ))}               </tbody>             </table>           </div>         )}          {/
Serviços
/}         {servicos.length > 0 && (           <div className="bg-white rounded-xl shadow-sm p-5">             <h2 className="font-bold text-gray-800 mb-3">Serviços</h2>             <table className="w-full text-sm">               <thead>                 <tr className="border-b border-gray-200">                   <th className="text-left py-2 text-gray-400 font-medium">Código</th>                   <th className="text-left py-2 text-gray-400 font-medium">Descrição</th>                   <th className="text-right py-2 text-gray-400 font-medium">Qtd</th>                   <th className="text-right py-2 text-gray-400 font-medium">Tempo</th>                 </tr>               </thead>               <tbody>                 {servicos.map((s) => (                   <tr key={s.id} className="border-b border-gray-50">                     <td className="py-2 text-gray-800">{s.codigo_servico || '—'}</td>                     <td className="py-2 text-gray-800">{s.descricao}</td>                     <td className="py-2 text-right text-gray-800">{s.quantidade}</td>                     <td className="py-2 text-right text-gray-800">{s.horas_padrao !== null ? `${s.horas_padrao} h` : 'Pendente'}</td>                   </tr>                 ))}               </tbody>             </table>             {tempoTotal > 0 && (               <div className="mt-3 pt-3 border-t border-gray-200 text-right">                 <p className="text-sm text-gray-600">Tempo total: <strong>{tempoTotal} h</strong></p>               </div>             )}           </div>         )}          {/
Aprovações
/}         {validacoes.length > 0 && (           <div className="bg-white rounded-xl shadow-sm p-5">             <h2 className="font-bold text-gray-800 mb-3">Aprovações</h2>             <div className="space-y-2">               {validacoes.map((v, i) => (                 <div key={i} className="flex items-center gap-3 text-sm">                   <span className={`px-2 py-1 rounded text-xs font-medium ${v.acao === 'APROVADO' ? 'bg-green-100 text-green-700' : 'bg-red-100 text-red-700'}`}>{v.acao}</span>                   <span className="text-gray-500 text-xs">{new Date(v.created_at).toLocaleString('pt-BR')}</span>                   {v.observacao && <span className="text-gray-600 text-xs">— {v.observacao}</span>}                 </div>               ))}             </div>           </div>         )}          {/
Botão imprimir
/}         <button           onClick={() => window.print()}           className="w-full py-3 bg-blue-600 text-white rounded-lg font-medium hover:bg-blue-700"         >           Imprimir / Salvar PDF         </button>       </main>     </div>   ) }
